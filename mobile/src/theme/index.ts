/**
 * MarketGPS Mobile - Theme Exports
 * 
 * Central export for all design tokens.
 * Import from '@/theme' to use tokens in components.
 */

export {
  colors,
  spacing,
  radius,
  typography,
  shadows,
  animation,
  theme,
  getScoreColor,
  getScoreBackgroundColor,
  type Theme,
  type Colors,
  type Spacing,
  type Radius,
} from './tokens';
